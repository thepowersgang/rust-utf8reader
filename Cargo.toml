[package]

name = "utf8reader"
version = "0.0.4"
description = "Provides a wrapper around Reader that returns a stream of UTF-8 characters"
license = "MIT OR Apache-2.0"
authors = ["John Hodge <tpg@mutabah.net>"]
